{
  "name": "address-sync",
  "version": "1.4.2",
  "description": "Address sync layer — Diagnostics Vertical, Tata 1MG",
  "main": "src/index.js",
  "private": true,
  "scripts": {
    "dev": "node src/index.js",
    "start": "NODE_ENV=production node src/index.js",
    "test": "jest --coverage",
    "validate": "node -e \"const v = require('./src/validator'); console.log(v.validatePinCode(process.argv[1]))\" --",
    "investigate": "node scripts/investigate.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "AnimeshJhawar",
  "repository": {
    "type": "git",
    "url": "https://github.com/AnimeshJhawar/addressSync.git"
  }
}
